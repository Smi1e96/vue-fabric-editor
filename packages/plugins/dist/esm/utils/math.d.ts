/**
 * 获取多边形顶点坐标
 * @param edges 变数
 * @param radius 半径
 * @returns 坐标数组
 */
declare const getPolygonVertices: (edges: number, radius: number) => {
    x: number;
    y: number;
}[];
export { getPolygonVertices };
